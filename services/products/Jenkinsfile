pipeline {
    // ============================================================
    // ⚙️ Agent & Options
    // ============================================================
    agent any
    options {
        timestamps()                       // Thêm timestamp vào log
        ansiColor('xterm')                 // Màu sắc log dễ đọc
        timeout(time: 30, unit: 'MINUTES') // Chặn treo pipeline quá lâu
        buildDiscarder(logRotator(numToKeepStr: '15')) // Giữ lại 15 bản build gần nhất
    }

    // ============================================================
    // 🧰 Tools (chỉ cần JDK17 cho SonarScanner nếu dùng sau này)
    // ============================================================
    tools {
        jdk 'jdk17'
    }

    // ============================================================
    // 🌍 Environment
    // ============================================================
    environment {
        // Trivy: fail khi có HIGH/CRITICAL (đúng tinh thần DevSecOps)
        TRIVY_EXIT_CODE = '1'
        TRIVY_SEVERITY  = 'HIGH,CRITICAL'

        // Bật BuildKit để build Docker nhanh & tối ưu layer
        DOCKER_BUILDKIT = '1'

        // Tên image cố định cho service
        IMAGE_NAME = 'products'
        // Tag sẽ được tính trong stage "Compute Image Tag"
        IMAGE_TAG  = ''
        // Đường dẫn virtualenv
        VENV_DIR   = '.venv'
    }

    stages {
        stage('0. Pre-Clean') {
            steps {
                echo '--> Dọn dẹp thư mục làm việc trước khi chạy'
                deleteDir()
            }
        }

        stage('1. Checkout SCM') {
            steps {
                echo '--> Lấy mã nguồn từ Git...'
                checkout scm
                sh 'git --no-pager log -1 --pretty=oneline || true'
            }
        }

        stage('1.1 Compute Image Tag') {
            steps {
                script {
                    // Lấy short SHA (7 ký tự). Fallback sang BUILD_NUMBER nếu repo không có git metadata.
                    def shortSha = sh(returnStdout: true, script: "git rev-parse --short=7 HEAD || echo ${env.BUILD_NUMBER}").trim()
                    env.IMAGE_TAG = shortSha
                    echo "--> IMAGE_TAG = ${env.IMAGE_TAG}"
                }
            }
        }

        stage('2. Unit Test') {
            steps {
                // Thêm dir() ở đây
                dir('services/products') {
                    echo '--> Tạo virtualenv và chạy pytest...'
                    sh '''
                        python3 -m venv ${VENV_DIR}
                        . ${VENV_DIR}/bin/activate
                        python -m pip install --upgrade pip
                        pip install -r requirements.txt
                        pytest -q
                    '''
                }
            }
        }

        // ========================================================
        // 🔐 Security Scans (local-first)
        // ========================================================
        stage('3. SonarQube Analysis') {
            steps {
                echo '[CHỜ] Sẽ cấu hình sau khi SonarQube sẵn sàng (kết nối scanner + quality gate).'
                // withSonarQubeEnv('SonarQube-Server') {
                //     sh 'sonar-scanner -Dsonar.projectKey=products -Dsonar.sources=app -Dsonar.python.version=3.11'
                // }
            }
        }

        stage('4. Trivy FS Scan (SCA)') {
            steps {
                // Thêm dir() ở đây
                dir('services/products') {
                    echo '--> Quét thư viện trong workspace với Trivy (Filesystem)...'
                    sh "trivy fs --no-progress --severity ${TRIVY_SEVERITY} --exit-code ${TRIVY_EXIT_CODE} ."
                }
            }
        }

        // ========================================================
        // 🐳 Build & Scan Container Image
        // ========================================================
        stage('5. Build Docker Image') {
            steps {
                // Thêm dir() ở đây
                dir('services/products') {
                    script {
                        def full = "${env.IMAGE_NAME}:${env.IMAGE_TAG}"
                        echo "--> Build image: ${full}"
                        sh """
                            docker build \
                              --pull \
                              -t ${full} \
                              .
                        """
                    }
                }
            }
        }

        stage('6. Trivy Image Scan') {
            steps {
                script {
                    def full = "${env.IMAGE_NAME}:${env.IMAGE_TAG}"
                    echo "--> Trivy image scan: ${full}"
                    sh "trivy image --no-progress --severity ${TRIVY_SEVERITY} --exit-code ${TRIVY_EXIT_CODE} ${full}"
                }
            }
        }

        // ========================================================
        // ☁️ Các bước chờ kích hoạt khi có AWS
        // ========================================================
        stage('[CHỜ] Push to ECR') {
            when { expression { return false } } // bật khi sẵn sàng
            steps { echo 'Sẽ đẩy image lên AWS ECR (login ECR, tag, push)...' }
        }

        stage('[CHỜ] Update GitOps Repo') {
            when { expression { return false } } // bật khi sẵn sàng
            steps { echo 'Sẽ cập nhật repo cấu hình để trigger ArgoCD...' }
        }
    }

    // ============================================================
    // 🧹 Cleanup
    // ============================================================
    post {
        always {
            echo '--> Dọn dẹp workspace & docker images...'
            script {
                def full = "${env.IMAGE_NAME}:${env.IMAGE_TAG}"
                sh """
                    docker rmi ${full} || true
                    docker image prune -f || true
                """
            }
            cleanWs()
        }
    }
}
