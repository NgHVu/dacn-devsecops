# ================================================================= #
# SERVER CONFIGURATION
# ================================================================= #
# Cổng riêng cho user service
server.port=8082
server.address=0.0.0.0
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=30s
server.compression.enabled=true
server.compression.min-response-size=1KB
server.compression.mime-types=application/json,text/plain,text/css,application/javascript,text/html
server.forward-headers-strategy=framework

# ================================================================= #
# ACTUATOR / HEALTH / METRICS
# ================================================================= #
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.probes.enabled=true
management.endpoint.health.group.liveness.include=livenessState
management.endpoint.health.group.readiness.include=readinessState,db

# ================================================================= #
# LOGGING & OTHERS
# ================================================================= #
logging.file.name=
spring.main.banner-mode=off

# ================================================================= #
# CORS CONFIGURATION (Sử dụng bởi WebConfig.java)
# ================================================================= #
# Cho phép frontend (ví dụ: chạy ở port 3000) gọi đến
app.cors.allowed-origins=http://localhost:3000

# ================================================================= #
# OPENAPI / SWAGGER-UI
# ================================================================= #
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.writer-with-default-pretty-printer=false

# ================================================================= #
# DATABASE
# ================================================================= #
# Cấu hình linh hoạt: ưu tiên biến môi trường, nếu không có sẽ dùng giá trị mặc định
spring.datasource.url=${DB_URL:jdbc:postgresql://users-db:5432/users_db}
spring.datasource.username=${DB_USER:admin}
spring.datasource.password=${DB_PASSWORD:admin}
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=true

# Cấu hình Spring Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nhoangvu2306@gmail.com
spring.mail.password=knfz pbig grvg yitz

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cấu hình nghiệp vụ
app.otp.expiration-minutes=10

# ================================================================= #
# JSON WEB TOKEN (JWT)
# ================================================================= #
# TỐI ƯU: Cấu hình linh hoạt cho JWT, sẵn sàng cho môi trường production
app.jwt.secret-key=${JWT_SECRET_KEY:dGhpcy1pcy1hLXNlY3JldC1rZXktZm9yLWp3dC1hdXRoZW50aWNhdGlvbi1pbi1kZXZzZWNvcHMtcHJvamVjdC1tdXN0LWJlLXZlcnktbG9uZy1hbmQtc2VjdXJl}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ================================================================= #
# INFO / BUILD METADATA
# ================================================================= #
management.info.env.enabled=true
info.app.name=users-service
info.app.version=@project.version@
